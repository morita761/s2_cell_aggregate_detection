# boxplot config  (INI format — read by Python's configparser)
# Lines starting with '#' or ';' are comments.
#
# Usage:
#   python3 tools/boxplot.py tools/config_example.txt output_a/ output_b/ output_c/
#
# label1 = first folder argument, label2 = second, ... (up to label6)

[labels]
label1 = trafec4d_on
label2 = almi_off
label3 = trafec4d_off
label4 = trafec5d_off
label5 = trafec6d_off
; label6 = Treatment E

[comparisons]
# 比べたいラベルの番号を指定 (label番号 , label番号)
# 省略時は隣接グループ間 (1v2, 2v3, ...) を自動で比較する
pair1 = 1,2
pair2 = 1,3
pair3 = 1,4
pair4 = 1,5

[plot]
title = S2 Cell Aggregation Size

# output: save to file instead of showing on screen.
# Supported extensions: .png  .pdf  .svg
output = compareredlipon-offoff.png
